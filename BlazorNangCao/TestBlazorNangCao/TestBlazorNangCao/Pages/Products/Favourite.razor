@page "/favourites"
@inherits FavouriteBase

<h3>Favourite Products</h3>

<input type="text" class="input-group-sm" @bind="SearchString" placeholder="search something" />

<div class="wrapper">
    @foreach (var prod in FavouriteProducts)
    {
        <ShowProduct Prod="prod"></ShowProduct>
    }
</div>